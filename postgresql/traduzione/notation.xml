<?xml version="1.0" encoding="UTF-8"?>
<!-- $PostgreSQL$ -->

<sect1 id="notation">
 <title>Convenzioni</title>

 <para>
  Questo libro usa le seguenti convenzioni tipografiche per marcare
  certe porzioni di testo: nuovi termini, frasi estere, ed altri
  importanti passaggi sono evidenziati in <emphasis>italics</emphasis>.
  Tutto ciò che rappresenta input o output del computer, 
  comandi particolari, codice di programma, e output su schermo, è
  visualizzato in un font monospaziato (<literal>esempio</literal>). Dentro tali
  passaggi, italics (<replaceable>esempio</replaceable>) indica un
  suggerimento; bisognerà inserire il valore reale al posto del suggerimento.
  Occasionalmente, parti di codice di programma sono evidenziati in grassetto
  (<emphasis role="bold"><literal>esempio</literal></emphasis>), se sono stati
  aggiunti o sono stati cambiati dal precedente esempio.
 </para>

 <para>
  Le convenzioni seguenti sono usate nella sintassi di un comando: 
  le parentesi quadre (<literal>[</literal> e <literal>]</literal>) indicano
  parti opzionali. (Nella sintassi di un comando Tcl, viene invece utilizzato
  il punto interrogativo (<literal>?</literal>), come è normale in Tcl). Le
  parentesi graffe (<literal>{</literal> e <literal>}</literal>) e le linee verticali
  (<literal>|</literal>) indicano che bisogna scegliere una
  alternativa. I punti (<literal>...</literal>) indicano che l'elemento precedente
  può essere ripetuto.
 </para>

 <para>
  Dove migliora la chiarezza, i comandi SQL sono preceduti dal   
  prompt <literal>=&gt;</literal>, e i comandi della shell sono preceduti dal
  prompt <literal>$</literal>. Normalmente, i prompt non vengono però mostrati.
 </para>

 <para>
  Un <firstterm>amministratore</firstterm> è generalmente una persona che è
  incaricata di installare e far funzionare il server. Un <firstterm>utente</firstterm>
  è chiunque stia usando, o desidera usare qualsiasi parte del sistema  
  <productname>PostgreSQL</productname>. Questi termini non dovrebbero essere interpretati
  in maniera restrittiva; Questo libro non fa previsioni sulle procedure di
  amministrazione del sistema.
 </para>
</sect1>
